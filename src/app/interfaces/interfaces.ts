export interface Componente {
  icon: string;
  name: string;
  redirecTo: string;
}
export interface Heroe {
  superhero: string;
  publisher: string;
  alter_ego: string;
  first_appearance: string;
  characters: string;
}
